import React from 'react';
import Particles from 'react-tsparticles';

const ParticlesComponent = ({ options }) => {
  return <Particles id="tsparticles" params={options} />;
};

export default ParticlesComponent;
